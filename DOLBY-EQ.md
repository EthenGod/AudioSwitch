# Dolby 10 点与 20 段换算

适配依据为本机 Dolby Access 3.27.12250.0 与当前 DAX 输出设备，不是公开的跨厂商 Dolby SDK。原生连接仍沿用已有的组件版本校验。

## 对照方式

2026-09-24 查看本机 Access 自定义界面，确认频率标签为 32、64、125、250、500 Hz、1、2、4、8、16 kHz，范围 ±12 dB。读取后台原始 20 段后，临时调整 32、64 Hz 和 16 kHz 控件，再读取后台作对照。试验结束恢复原 20 段，并独立读回全部 Dolby 参数确认完全一致；Access 界面也回到原曲线。

安装组件里的 48 kHz 频率表提供了以下插值位置；真实曲线与高频端调整结果均符合这些位置。这里的插值位置与界面频率标签不是一回事，不能直接把 32 Hz 等标签代入后面的 20 段表。

```
20 段插值位置：
47, 141, 234, 328, 469, 656, 844, 1031, 1313, 1688,
2250, 3000, 3750, 4688, 5813, 7125, 9000, 11250, 13875, 19688

10 点对应的 20 段索引（从 0 开始）：
0, 1, 2, 3, 4, 7, 10, 13, 16, 18
```

10→20：各点的 dB 乘 16，取最近的整数。在相邻锚点之间按上述位置做线性插值。最后一段沿最后两个锚点外推，最终限制在 −192 到 192。实现明确采用半格远离零的舍入方式；本机样本已逐段核对，但未穷尽 Access 的所有半格输入、设备、采样率和版本。

20→10：读取这些锚点并除以 16。20 段的任意中间细节不能全部装进 10 个点；这是有损投影，不是两段求平均，也不承诺任意曲线往返不变。

## 本机对照数据

原始后台曲线：

```
-27,16,26,56,77,60,43,26,25,24,23,40,56,77,51,21,-22,-31,-42,-66
```

10 个锚点的原始增益是 `-27,16,26,56,77,26,23,77,-22,-42`，分别除以 16 得到 dB。这组输入经换算与上面 20 个原值完全一致。

调整低频两个锚点至 173、189，高频最后一个锚点至 192 时，Access 后台读回：

```
173,189,26,56,77,60,43,26,25,24,23,40,56,77,51,21,-22,77,192,192
```

这也核对了高频插值、末段外推和限幅。两组实测数据均作为固定回归样本，另外测试平直曲线、上下限、小数、无效输入与 1000 组随机曲线。

## 编辑与配置

`DolbyEqualizerEditor` 以 20 段为唯一保存值。载入和切换视图只生成显示值；实际编辑 10 点才重新插值。直接修改高级视图只更新对应原始频段及 10 点显示。dB 显示两位小数，未编辑的内部锚点保留完整精度。

10 点视图提供竖向滑块与同步数值框，范围 −12 至 +12 dB。拖动滑块调整对应频段；方向键每次微调 0.1 dB，Home / End 到达下限／上限，也可直接输入两位小数。按下滑块后原地松开不会把未编辑的锚点重新量化。高级视图保留 20 个原始整数输入，不将驱动单位标为 dB。滚动区域使用主题滚动条，滚动本身不修改曲线。

配置继续使用 `Dolby.Eq`，不改变备份版本。保存、自动应用、读取、导入导出走已有路径；该功能只在短时界面进程里工作，不新增后台轮询或常驻资源。
