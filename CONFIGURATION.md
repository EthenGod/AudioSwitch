# 配置与备份

唯一活动用户配置：`%LOCALAPPDATA%\AudioSwitch\settings.json`。
程序自动保存、导出备份、导入恢复共用同一种 UTF-8 JSON 格式。
`AudioSwitch.exe.config` 是 .NET 运行时文件，不存放用户设置。

“全部设备 / 声音输出 / 麦克风输入”筛选仅在当前窗口内保留，不写入此文件，也不会改变音频设置；重新打开面板默认显示全部设备。深色模式通过新增的 `DarkMode` 布尔字段保存，导入导出及界面状态同步包含此字段。版本仍为 1；缺少字段的旧配置默认为浅色，新备份需用支持 `DarkMode` 的新版导入。

EQ 滑块不增加配置字段：`Dolby.Eq` 仍保存 20 个驱动单位整数。只有用户修改 10 点 dB 值时才使用现有换算生成 20 段；仅查看、切换视图、滚动或原地按下松开滑块都保留原值。“仅保存”不写入实际音频设备。

托盘右键菜单、下拉列表及提示窗口也使用 `DarkMode` 外观设置；样式更新不新增配置字段或改变备份版本。

```json
{
  "Format": "AudioSwitch.Settings",
  "Version": 1,
  "Settings": {
    "AskOnConnect": true,
    "IncludeCommunications": true,
    "UseDevicePriority": true,
    "DarkMode": false,
    "DeviceProfiles": {},
    "DeviceOrder": [],
    "DeviceRules": {}
  }
}
```

字段含义：

- `AskOnConnect`：新设备接入时询问。
- `IncludeCommunications`：同时切换通话设备。
- `UseDevicePriority`：按优先级自动选择设备。
- `DarkMode`：`true` 为深色，`false` 为浅色；只能是布尔值，不接受字符串、数字或 `null`。仅影响外观，不切换设备、不改变音量、空间音效或 Dolby。
- `DeviceOrder`：设备列表，元素包含 `Id`、`Name`、`Flow`。`Flow` 为 0 表示输出、1 表示输入；同类设备越靠前越优先。离线设备也保留。
- `DeviceProfiles`：按设备 ID 保存预设。`Volume` 为 0–100 整数，`null` 表示保持当前音量；`SpatialFormat` 为音效 GUID，空字符串表示关闭，`null` 表示保持当前音效。
- `DeviceRules`：按设备 ID 保存白名单。0 表示普通设备、1 表示接受系统选择、2 表示接入自动切换。

v0.9 在每个 `DeviceProfiles` 项内增加可选的 `Dolby` 对象。旧文件不含此字段时视为关闭；设为 `null` 也关闭自动应用。它和音量、空间音效使用同一份配置和备份，不另建 Dolby 用户配置。版本仍为 1；旧版声间不认识新增字段，因此请用 v0.9 或以上导入新的备份。

Dolby 对象中的各字段为 `null` 时保持当前值：

- `MainProfile`：0 为电影，4 为自定义；选择自定义时必须带 `SubProfile`，4 为自定义 1、5 为自定义 2。其他模式待验证后开放。
- `Enabled`、`Surround`、`Dialog`、`Leveler`、`AutoSwitch`：分别为 Dolby 总开关、环绕、人声增强、音量平衡、Dolby 内容自动切换，值为布尔值。
- `SurroundStrength`、`DialogStrength`、`LevelerStrength`：0–1 的强度，界面显示百分比。驱动可能按档位取整，实际不同会在面板说明。
- `Ieq`：电影模式下 0 为关闭、2 为平衡。
- `Eq`：自定义模式的 20 个整数，从低频到高频，范围 −192 到 192；这是驱动单位，不是 dB。

v0.10.0 的 10 点 dB 编辑器继续使用同一个 `Eq` 字段，配置版本仍为 1，不新增 `Eq10` 或第二份配置文件。10 点修改先转换为 20 个整数再保存；打开、读取、切换视图和未经编辑的保存不会把旧曲线降为 10 点。任意 20 段曲线转为 10 点显示有信息损失，因此仅在实际编辑 10 点后重新插值。10 点控件范围 ±12 dB，映射锚点以 16 个驱动单位对应 1 dB，完整方法见 `DOLBY-EQ.md`。导入导出仍按原始 20 段无损保存，v0.9 及以上可读取。

不同设备使用同一 Dolby 自定义槽位时，只有保存了 `Eq` 等具体参数才能恢复各自的调音；仅保存槽位编号会使用该槽位当时的内容。“读取当前 Dolby 并填入”可一次保存当前支持模式的全部已验证参数。

Dolby 随日常默认输出（多媒体角色）变化而应用，也适用于启动、优先级回退、白名单和系统外部切换。仅通话设备改变不会改变 Dolby。接受系统选择白名单仍保留原有音量／Windows 空间音效；显式开启的 Dolby 自动方案独立生效。未设置 Dolby 的设备不会主动重置驱动的全局音效。

导入是完整替换。旧配置先保存到同目录的 `backups/before-import-时间-编号.json`，导入该文件即可恢复；旧版自动迁移前生成 `before-migration-时间-编号.json`。不会自动清理备份。

导入不会立即切换设备，也不改变当前音量或音效。导入的偏好和白名单立即参与后续事件处理，预设在下次选择设备时应用。待处理的旧设备提示会清除。错误提示、待处理事件、进程信息和日志不属于备份设置。

v0.9.1：导入或仅保存当前设备时，会取消尚未完成的旧 Dolby 任务。旧任务停止后续写入；如果已写入部分参数且原设备仍是当前输出，会尝试恢复这部分修改。不会主动应用新导入／仅保存的方案。设备暂时读不到音量或空间音效时，编辑其他设置会保留原字段；保存只校验配置本身，真正应用时再检查当前驱动能力。

v0.10.1 不改变配置格式。Dolby 读取期间暂停编辑；返回空结果、无效参数或检测到预设切换时保留原方案并提示重试。驱动原始强度／均衡器无效时，不将其用作恢复值继续覆盖；恢复读回失败也不会报告成功。

设备按 Windows 端点 ID 匹配，不按名称强行套用；换电脑或重新安装驱动后，ID 可能变化，无法匹配的记录仍保留，但不会误用于同名设备。导入时不要求设备在线，也不因当前未安装某音效而删除预设。

支持旧版直接包含偏好字段的 JSON 文件。新格式版本号用于后续兼容判断，不支持的版本不会自动降级。文件限 1 MiB；保存时先写临时文件再替换，校验或备份失败不会覆盖现有设置。
