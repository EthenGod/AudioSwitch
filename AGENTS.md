# 声间 / AudioSwitch 项目协作约定

适用范围：本目录及其子目录。与用户沟通尽量用大白话，先说结果，再说明影响和验证情况。不要把“编译通过”说成“实际插拔已经验证”。

## 文件安全

- **禁止批量删除文件或目录。** 不使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`，也不能用循环或脚本绕过限制。
- 确实需要删除时，只能一次删除一个明确路径的文件，例如 `Remove-Item -LiteralPath 'C:\path\to\file.txt'`。需要批量清理时停止清理，请用户手动删除。
- 不清空用户配置、备份、日志或 Dolby 设置来“解决”问题。不要改 `.codex/`、其他项目、无关进程或系统驱动文件。
- 先检查已有改动；保留用户工作。`bin/`、`staging/`、`artifacts/` 为构建／验证输出，不作为源码提交。不要擅自初始化、提交或重置 Git 历史。

## 产品目标与技术边界

- Windows x64 音频设备管理工具，原生 WinForms 界面。保持轻量，不引入 Electron、WebView、常驻浏览器或新的后台服务。
- 使用 Windows 自带的 .NET Framework C# 编译器，保持 **C# 5** 语法和现有 .NET Framework 兼容性。入口为 `src/Program.cs`；没有 SDK 风格项目或 NuGet 恢复步骤。
- 默认进程是托盘后台；`--ui` 是面板，`--prompt` 是右下角提示，`--dolby-worker` 是短时辅助进程。关闭界面必须释放对应进程，不能只是隐藏。
- 空闲后台依靠 Core Audio 事件，不轮询设备或 Dolby。约 150 ms 的事件合并从首条事件起算，连续事件不能无限推迟处理。
- 中文界面沿用 `Palette` / `FlatAction`，提示要说明可执行的操作。不要把调试协议、RPC 编号或内部实现细节放到普通使用流程里。

## 代码地图

| 文件 | 负责内容 |
| --- | --- |
| `src/TrayHost.cs` | 后台生命周期、IPC 请求、音频事件、配置、自动应用与提示 |
| `src/Models.cs` | IPC 数据模型、偏好、设备变化跟踪器 |
| `src/DeviceAutomation.cs`、`DevicePriority.cs` | 白名单、优先级和自动选择的纯逻辑 |
| `src/DeviceSelection.cs`、`DeviceProfiles.cs` | 默认设备切换、预设应用、失败恢复 |
| `src/AudioInterop.cs`、`SpatialAudio.cs` | Core Audio / COM、Windows 空间音效和 SVCL |
| `src/DolbyProfiles.cs` | Dolby 参数校验、捕获、事务与恢复 |
| `src/DolbyWorker.cs`、`DolbyNative.cs` | 可取消任务队列、独立进程、固定版本的 DAX RPC 适配 |
| `src/PreferenceStore.cs`、`Ipc.cs` | 统一配置／备份、当前用户与会话隔离的命名管道 |
| `src/Dashboard.cs`、`DevicePrompt.cs`、`*Dialog.cs`、`DolbyEditor.cs` | 面板、提示、设备设置、排序及 Dolby 编辑器 |
| `tests/Tests.cs` | 无外部测试框架的回归测试、界面渲染和可选实机验证 |

## 必须保留的行为

- 输出与麦克风分别处理；角色 0 为控制台、1 为多媒体、2 为通话。“同时切换通话设备”关闭时，不写角色 2。
- 设备按 Windows 端点 ID 匹配，不按显示名称强行匹配。离线设备保留排序和预设；同名设备不能套用彼此配置。
- 优先级默认开启，关闭后保留原系统默认选择逻辑。普通手动切换不能被后续重复通知立即改回。
- 白名单先于普通优先级和弹窗判断。`AcceptSystem` 接受 Windows 结果，不改音量／Windows 空间音效；`SwitchOnConnect` 接入后主动应用并切换。输入和输出免打扰互不影响。
- 用户显式开启的 Dolby 方案独立跟随多媒体默认输出，包含白名单、启动、断开回退及系统外部切换。只有通话角色变化时不应用 Dolby。未配置 Dolby 的设备不主动重置驱动全局效果。
- 音量／Windows 空间音效在路由前应用，Dolby 在路由后应用。真实应用必须检查结果；失败时恢复**已尝试写入**的设置，不能因写调用抛异常就假定没有变化。恢复也要核验，恢复失败不能说“已恢复”。
- “仅保存”和导入不启动新的音频写入；实际应用时再检查驱动支持情况。暂时读取失败不能导致保存原预设时清空字段。
- Dolby 队列串行执行，只保留最新待处理任务。切换、取消、导入、关闭后台需要让在途任务停止继续写入，不能只忽略回调。取消后仅在原设备仍是当前输出时恢复已写入值。
- 白名单下可跳过不支持的空间效果并保留配置；Dolby 失败在面板显示可关闭说明，不启动提示界面。真实路由失败仍要说明原因。
- 提示已处理完后应退出，历史错误不能让空提示常驻。关闭面板提示条后，不应被相同的后台快照立即重新显示。

## 配置与 Dolby 约束

- 唯一活动配置为 `%LOCALAPPDATA%\AudioSwitch\settings.json`。日常保存、导出、导入走 `PreferenceStore`，格式以 `CONFIGURATION.md` 为准；`AudioSwitch.exe.config` 只是运行时配置。
- 配置需严格校验类型、范围、版本和大小，最多 1 MiB；先写临时文件再替换。导入替换前保存可恢复的完整备份；不自动清理备份。不支持的设备／效果仍保留配置。
- 新增配置字段同时考虑旧配置加载、IPC 序列化、导入导出、编辑器保存和回归测试。`null` 的“保持不变”含义不能与关闭、零值混用。
- Dolby 是已验证本机组件的版本固定适配，不是公开通用 SDK。未知组件校验值应停止操作并说明，不得为了兼容随便移除校验或猜测协议偏移。
- 辅助进程 JSON 标准输入／输出统一 UTF-8；不要依赖控制台默认代码页，否则中文错误可能乱码。取消信号与错误文本应有真实跨进程测试。
- `InitializeEx` 接受某个 GUID 不代表设备支持 DAX；必须先用 `IsDaxEndpoint` 验证。DAX 使用 MMDevice ID 最后一个带花括号的 GUID。
- 不读取或修改 Dolby 授权／激活信息，不直接编辑其 Runtime XML，不复制分发其 DLL，不重置用户自定义槽位。通过独立进程隔离原生调用，保留超时和写前设备检查。
- 配置里的均衡器仍是 20 个驱动单位整数，原始编辑器不能直接标为 dB。`DolbyEqualizer` 提供已用本机 Access 曲线核对的 10 点 dB 换算，详见 `DOLBY-EQ.md`；不要按相邻两段复制／平均。查看或切换编辑视图不能重新生成原曲线，只有用户修改 10 点时才转换。强度可能被驱动按档位取整。未验证的模式或功能不要伪装成已支持。
- 保留 `vendor/svcl` 的版本校验、许可与 `THIRD_PARTY.md`。不要未经验证替换系统音效辅助组件。

## 构建与验证

在项目根目录执行，优先使用 `staging` 避免覆盖正在运行的程序：

```powershell
.\build.ps1 -Test -OutputDirectory staging
.\staging\AudioSwitch.Tests.exe --render
```

- 普通测试包含纯逻辑、临时测试配置及真实设备的只读枚举，不改变真实音频设置。`--render` 额外渲染界面至 `artifacts/`；界面修改后查看相应图片，检查裁切、遮挡及按钮可用性。
- `--worker-cancel` 运行带预先触发取消信号的辅助进程，验证跨进程取消及中文错误编码；在进入 Dolby 原生读写之前退出，不更改真实音频设置。
- 修复逻辑问题时补能够复现故障的测试，重点检查重复通知、快速切换、部分写入失败、恢复失败、取消、空设备和旧配置。不要写只重复实现细节的测试。
- 以下是**可选的有副作用测试**，先理解其条件和恢复逻辑，再根据当前任务授权决定是否执行；不要把它们默认绑到每次构建：
  - `--lifecycle` 会运行真实后台及已开启的启动优先级，验证界面退出、重新加载和 IPC。
  - `--settings-smoke` 会把非默认设备当前音量／空间音效原值写回。
  - `--dolby-smoke` 会临时保存并应用电影方案，再恢复配置和原 Dolby 参数；要求受支持的当前输出和一致的三个输出角色。
- 生命周期／Dolby 实机测试前确认没有已有后台，避免测试连接到用户正在使用的实例。涉及停止进程时按可执行文件完整路径及 PID 限定，只处理本项目实例。
- .NET Framework 反射诊断使用 `powershell.exe`；PowerShell 7 加载 `System.Web` 序列化可能失败。检查原生命令的退出码，不要让管道尾部命令掩盖失败。
- 实机修改前保留原设置，`finally` 中恢复并独立读回。不要删除用户配置来换取测试通过；无法恢复要明确报告。
- 通过所需测试后再更新 `bin`。更新已运行程序时保留旧可执行文件，核对新构建后重启；确认空闲时只有托盘后台、没有遗留界面或辅助进程。
- 蓝牙／USB 实际插拔、睡眠唤醒、跨 DPI 等人工项目见 `MANUAL-TESTS.md`。自动模拟不代替实机验收。

## 交付说明

说明修复了什么、用户会看到什么变化、实际运行了哪些测试，以及尚未验证的范围。功能或配置行为变化时同步更新 `README.md`、`CONFIGURATION.md` 和必要的手动测试步骤；代码审查记录可放入 `CODE_REVIEW.md`。
