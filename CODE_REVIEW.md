# 代码检查记录 · v0.9.1

日期：2026-09-23。

本次检查覆盖后台请求与事件处理、优先级／白名单、设备接入与断开、默认角色切换、音量／空间音效预设、Dolby 队列与原生适配、配置导入导出、面板／提示生命周期、构建和测试入口。确认的问题已修复，未进行无关的框架替换或整体重构。

## 已修复问题

| 优先级 | 原问题与触发条件 | 修复与验证 |
| --- | --- | --- |
| P1 | Dolby 正在应用时，切换设备、仅保存、导入或退出后台，只会让旧回调失效；辅助进程仍可能继续写旧方案。 | 队列发送取消信号，跨进程命名事件传递；每次写入前检查取消、父进程和当前输出。取消后允许在原输出上恢复已写值。测试覆盖运行中取消、退出、快速切换、恢复，以及真实辅助进程在原生访问前收到取消信号。涉及 `DolbyWorker.cs`、`DolbyProfiles.cs`。 |
| P1 | 默认设备写调用先改变角色再抛异常时，该角色未加入恢复列表，留下部分切换状态。 | 在调用前记录已尝试的角色，失败后逆序恢复；注入“已写入再失败”的测试确认角色恢复。涉及 `DeviceSelection.cs`。 |
| P2 | 恢复默认设备时，只检查调用是否抛异常；Windows 未采用恢复值也可能显示“已恢复”。 | 恢复后重新读取角色，任何不一致或读取失败都报告未完全恢复。测试覆盖静默拒绝恢复。涉及 `DeviceSelection.cs`。 |
| P2 | 音量／空间音效临时读取失败，编辑器禁用控件后保存白名单等其他设置，会把原预设写成 null；保存路径还会因驱动不可用拒绝仅保存。 | 不可编辑字段保留原值；保存校验配置类型和范围，实际应用再做能力检查。界面测试模拟读取失败，验证白名单可改、音量和音效不丢。涉及 `DeviceSettingsDialog.cs`、`TrayHost.cs`。 |
| P2 | 右下角提示曾经操作失败，之后待处理事件已被白名单或其他窗口清除，旧错误仍阻止提示退出。 | 后台确认无待处理项且无当前错误时直接退出，不受旧本地错误影响。回归测试覆盖先失败、后被外部处理的提示。涉及 `DevicePrompt.cs`。 |
| P2 | Dolby 内容自动切换在主预设之后才关闭，或在其他效果写入前开启，可能在调音期间改变选中的预设。 | 显式关闭放在选预设前；显式开启放在其余设置后。测试核对写入顺序。涉及 `DolbyProfiles.cs`。 |
| P2 | 无控制台辅助进程和父进程使用的编码不一致，Dolby 中文错误变为乱码。 | 标准输入／输出使用显式 UTF-8 流，父进程指定 UTF-8 解码；不对无控制台进程设置控制台代码页。真实跨进程测试验证“Dolby 任务已取消。”正确传输。涉及 `DolbyWorker.cs`。 |

另增加生命周期／Dolby 实机测试的后台互斥量预检查：检测到已有实例就退出测试，避免第二次启动命令意外打开用户面板。版本显示和应用清单统一为 v0.9.1。

## 验证结果

```powershell
.\build.ps1 -Test -OutputDirectory staging
.\staging\AudioSwitch.Tests.exe --render --lifecycle --dolby-smoke --worker-cancel
```

- 基础构建测试通过 186 项，最终合并测试通过 **272 项**。
- 真实后台切换到电影 Dolby 预设成功，独立辅助进程读回确认；之后恢复原用户配置及原 Dolby 参数，并重新读取验证。输出默认角色未变。
- 面板退出后对应进程结束，后台保持响应；提示独立启动并在处理完成后退出。该次后台工作集约 48 MiB、私有内存约 32 MiB，仅为本机测量值。
- 跨进程取消测试使用预先触发的事件，在 Dolby 原生读写前退出；写入中途取消及恢复由可控故障注入测试验证，未故意中断真实驱动 RPC。
- 最终测试日志：`artifacts/v0.9.1-final-tests.txt`。界面截图仍由 `--render` 输出到 `artifacts/`。
- 沙箱曾拒绝替换之前生成的测试文件；同一构建在普通 Windows 用户环境完成以上验证。中文编码问题是在真实跨进程测试中发现并修复的。

## 仍需实机验证的范围

- 本轮没有实际插拔 USB／蓝牙设备，也没有覆盖所有睡眠唤醒、驱动事件顺序或 DPI 组合；继续按 `MANUAL-TESTS.md` 验证。
- Dolby 适配仍只支持已验证的组件校验值、设备和模式；升级或不支持时保留配置并报告原因。
- 写前检查及取消属于协作式处理，不能中断已经进入驱动的单次调用。驱动超时、设备断开或外部程序同时更改音效时，恢复不能保证成功；普通失败路径会报告未完成，过时任务不会覆盖较新的完成结果。

这是一轮针对当前实现的检查与修复，不构成对所有驱动和硬件组合“没有任何 bug”的保证。后续维护规则见 `AGENTS.md`。

## 2026-09-24：v0.10.0 均衡器编辑

- 增加 10 点 dB 视图及与 20 段原始值的转换，保留单一 `Eq` 配置字段。视图切换和未编辑的保存不会损失原曲线细节。
- 本机 Access 两组实际曲线作为固定回归样本，覆盖中间插值及高频限幅；试验后恢复并独立读回原 Dolby 全部字段。推导范围及限制见 `DOLBY-EQ.md`。
- 基础测试通过 **200 项**，带 `--render` 共 **265 项**；检查了 10 点及高级视图截图、旧值保存、编辑同步、关闭 EQ 和统一备份。日志为 `artifacts/eq-tests.log`。本轮没有重复运行与该 UI 修改无关的插拔或生命周期实机测试。
- `bin/AudioSwitch.exe` 已更新并与 staging 校验哈希一致；旧可执行文件单独备份。用户当时开着设备设置窗口，为保留可能尚未保存的编辑，没有自动结束或重启现有进程，需要保存后从托盘退出并重新启动。

## 2026-09-24：v0.10.1 Dolby 专项复查

本轮检查 `DolbyProfiles`、`DolbyWorker`、`DolbyNative`、`DolbyEditor`、10/20 段转换和编辑器，并追踪 `TrayHost` 的设备切换／取消／保存路径及配置校验。修复以下问题：

1. **[P1] 取消发生在原生读取期间，仍会写入一次。** 原代码只在读取旧参数前检查取消和当前输出，读调用返回后直接写。新增故障注入先复现了该失败，再在读取返回后、写入前补检查，覆盖整数开关、强度、EQ、主预设／槽位；主预设恢复涉及第二次写入时也再检查输出。
2. **[P2] 读取方案可能混合不同预设的数据。** 原先先读主预设、后读效果、最后读槽位，Access 中途切换时仍能生成格式合法的混合方案。现在先记录主预设及槽位，读取结束再核对，不一致就提示重新读取。检测不等于驱动提供原子快照，不能保证发现外部程序快速切走再切回。
3. **[P2] 读取期间的用户编辑会被迟到结果覆盖。** 读取时原先只禁用读取按钮，仍可改参数。现在暂时锁定 Dolby 编辑区及自动应用开关；成功或失败都会解锁。空结果及无效方案不清空原配置。用可控延迟读取测试验证。
4. **[P2] 无效原值会进入恢复流程，NaN 恢复读回可能误报成功。** 新增对原始强度、EQ 长度与范围的检查。`Math.Abs(NaN)` 的比较不能识别恢复失败，现改为先检查有效性；故障注入确认明确报告“未能恢复”。未写入时不再笼统声称“已恢复”。

验证：基础测试 **213 项**；`--render --worker-cancel` 共 **285 项**，日志 `artifacts/dolby-review-tests.log`、`artifacts/dolby-review-render.log`。检查了渲染图及输入未失焦保存、视图切换不改曲线、20 段备份原样保留。10 点换算两组实测样本和 1000 组随机曲线继续通过；没有发现需修改换算公式的问题。

边界：本轮并未实际插拔 USB／蓝牙，也没有用真实驱动制造故障；异常与取消靠模拟注入及真实跨进程取消验证。不能中断已经进入驱动的单次写入。未扩展到其他 Dolby 版本／设备／采样率，原生协议的固定版本保护保持不变。

补充验证与交付：修复后的独立辅助进程在当前输出上只读获取主预设 4、槽位 4 和完整 20 段成功。确认界面已关闭且后台空闲后，备份旧文件并更新、重启 v0.10.1；核对新文件哈希，原配置文件和全部默认设备角色均未变化。新后台 PID 25396，空闲时无面板、提示或在途 Dolby 任务，面板错误／警告为空。

## 2026-09-26 下拉高光与界面构建小幅优化

- 下拉选项上下内边距由 4 改为 2 个逻辑像素，保留首尾边框留白；文字与对勾仍按整行居中。局部行缓冲继续包含边框，避免悬停擦除轮廓。
- Dolby 编辑器组装期间合并滚动画布布局与标签对齐；均衡器、主界面设备卡片批量添加子控件后统一布局。滚动中的同步刷新保持原行为。
- `build.ps1 -Test -OutputDirectory staging`：218 项通过；`AudioSwitch.Tests.exe --render`：465 项通过。检查了浅/深色下拉菜单、150% Dolby 布局、200% EQ 与紧凑主界面图片。
- 使用假设备设置窗口测量 40 次构建（预热 5 次）：前后中位数均约 51 ms，未测出稳定的整体耗时改善。此次仅说明减少重复布局工作，不声称打开速度提升某个百分比。
- 未运行真实音频写入或设备插拔测试。
