# 实机验收

## 新版界面专项检查

- 在设备设置中展开空间音效、白名单及 Dolby 下拉框，先不移动指针检查四条边线，再依次移过首项、中间项和末项。边框不能部分消失，四角不能越画越深，高光不能贴到外边线；重复展开和 Esc 收起后原选择应保留。
- 在深浅主题和不同缩放下，检查设备卡片的扬声器、麦克风图形在方块内居中，方块在设备行中垂直居中；Dolby 方案的预设、五个开关、智能均衡器及强度行左右文字应处于同一高度。
- 按住右侧滚动条缓慢拖动、快速拖动并中途反向；主面板设备列表和 Dolby 方案都应随手更新，不等松开才刷新。滚到 EQ 后再拖回顶部，检查无残影、编辑值不变；滚到边界继续拖动时不抖动。
- 深浅模式下反复展开下拉框，快速移动指针经过不同选项及同一选项内部；高光应稳定，不整张闪烁，对勾仅在已确认项。检查展开部分没有原生凹边框、方形内框或双重轮廓，四角和选项圆角完整；菜单在屏幕底边向上展开时也应完整。Esc 取消与方向键、Enter 选择保持可用。
- 从真实托盘图标打开右键菜单，确认沿用蓝底白色声波标志，与主面板和窗口图标一致，检查不同系统缩放下的清晰度。菜单应显示“声间”、当前输出和麦克风名称，无默认设备时有说明；检查两行设备文字、对勾和箭头对齐，项目不超出菜单边框，长名称省略后可悬停查看完整内容。检查屏幕边缘定位、子菜单展开和多设备列表滚动；子菜单不应出现额外空隙。切换主题后重新打开菜单，配色应跟随。选择设备、优先级及退出的原有行为仍需实机验收。
- 面板底部已有错误或音效说明时，检查整片黄色底已换成主题卡片，错误用红色小图标、提醒用黄色小图标。缩小窗口后“查看详情”和关闭按钮不遮挡正文；长消息可在详情中完整查看和复制。关闭后等待刷新、重开面板，原提示不应立即重现，新发生的问题仍应显示。检查深浅主题切换后卡片和图标周围没有异色方块。
- 检查异常提示、导入确认和完成提示的深浅色、长文本滚动、复制详情和关闭按钮。导入取消／关闭不替换设置；实际确认导入前应检查备份和恢复条件，不将有副作用的导入绑到外观自动测试。

- 用正式程序首次打开设备设置，不悬停任何控件，检查“应用此音量”和 Dolby 顶部复选框后面没有其他文字、下拉框或蓝色色块；切页后仍正常。滚动至 EQ 底部再滚回，检查文字和底色没有残影，按钮四角干净。此项必须看真实窗口，`DrawToBitmap` 截图曾漏掉系统主题下的背景残留。
- `AudioSwitch.Tests.exe --settings-layout` 可单独运行 100%、150%、200% 比例的布局模拟并输出截图；它不修改 Windows 缩放设置，也不代替真实 DPI 的人工检查。完整 `--render` 也包含此检查，测试程序现与正式程序共用 DPI 清单和视觉样式初始化。

- 切换左侧“深色模式”，再关闭、重开面板；检查主题保留，实际声音不变。检查设置页、Dolby 均衡器、优先级列表和右下角提示的配色。分别导出、切换、导入，确认主题随备份恢复。
- 深浅模式下用鼠标与键盘展开空间音效、白名单和 Dolby 下拉框；检查当前项标记、方向键选择、Esc 收起、文字对比度。设备很长或选项很多时应可滚动选择。
- 在上述下拉菜单中，仅将指针依次移入多个未选项再移出，不点击；对勾应始终只有一个，留在已确认项。按 Esc 收起后原选择不变，点击另一个选项后对勾才跟随新选择。
- 在 Dolby EQ 中拖动每个滑块并输入小数，检查对应数值同步；方向键每次调 0.1 dB，Home / End 到达 ±12 dB 边界。未启用 EQ 时滑块和数值框应禁用。高级视图仍为 20 个原始整数，不能标作 dB。
- 加载带有中间频段细节的 20 段曲线，来回切换视图、滚动、按下滑块后原地松开，再仅保存；原始 20 段应完全保留，当前声音不变。实际修改一个 10 点数值后，才应按换算规则生成新曲线。
- 在 Dolby 页用滚轮、拖动主题滚动条和 Tab 导航到页底；控件应完整露出，编辑值保留。分别在深浅模式检查复选框、数值框边框和箭头，不应出现重叠边框或突兀白色滚动条。
- 按下按钮后移出边界再松开，确认不误触发；Tab 焦点、禁用、悬停和按下应能区分。排序列表选中后移动，选中项应继续跟随原设备。

- 将面板缩到最小，再最大化；使用多台设备和很长的名称，确认只需纵向滚动，名称不遮住“切换至此”和“设备设置”，悬停能读到完整名称。
- 在输出、输入和全部设备之间切换，确认筛选本身不改变实际声音；等待后台刷新后仍保留当前筛选。在只看输出时接入麦克风，仍应显示接入提示。
- 用 Tab 在开关和按钮之间移动，按空格操作，检查焦点框和按下反馈；在设备设置里用 Ctrl+Tab 切页，检查编辑值保留，Esc 可以在空闲时关闭。
- 在 100%、150%、200% 缩放和不同 DPI 显示器之间检查面板、设备设置、Dolby 均衡器、优先级和提示浮窗；自动渲染检查不能代替这项实机验收。
- 关闭和重开面板，确认界面进程退出、托盘仍工作，配置和 Dolby 方案不因界面升级变化。

## 音频及生命周期

普通自动测试及 `--render` 不会更改电脑音频设置。`--lifecycle` 会运行真实后台及已开启的启动优先级逻辑；`--settings-smoke` 会把非默认端点设置原值写回。下面这些需要实际设备配合验证。验证旧版系统选择行为时，先关闭“按设备优先级选择”。

1. 启动声间，确认输出和输入列表与 Windows 声音设置一致，现有设备不触发新设备提示。
2. 播放音频，切换两个输出设备，检查 Windows 默认值及实际声音。切换麦克风，检查 Windows 输入默认值。
3. 关闭“同时切换通话设备”，切换输出，确认通话默认值保留。重新开启，确认三种角色切换到目标设备。
4. 记录当前默认设备，接入 USB 耳机。输出和输入都应出现选择；点击“继续用旧设备”，检查恢复；拔出再插入，点击“使用新设备”，检查切换。
5. 新设备提示出现后直接拔出新设备：如果它尚未成为默认设备，接入提示应消失；如果它已成为默认设备，应改为断开提示。拔出旧设备时，恢复按钮应禁用或改为断开提示。
6. 快速接入两个输出设备，两者都应保留为可选项；反复插拔不应堆积重复提示。
7. 点击“保持系统当前选择”，Windows 当前选择保持原样。只关闭窗口后重新打开，待处理选择应还在。
8. 关闭“新设备接入时询问”，重新插入设备，只更新列表而不弹窗；退出重启后设置仍保留。
9. 从托盘打开窗口，在任务管理器中应看到两个 AudioSwitch 进程；关闭或最小化窗口后只剩后台，双击托盘可重开。重复十次后检查占用是否持续增长。
10. 在 100%、150%、200% 系统缩放下检查长设备名称、错误信息及滚动区域；分别检查空设备列表和多设备列表。
11. 右键托盘退出，确认两个进程都退出，托盘图标消失。运行多个启动实例时后台应保持单实例。
12. 蓝牙断连重连、睡眠唤醒后检查设备更新。若重启 Windows Audio 服务，使用“刷新”确认恢复或显示明确错误。
13. 当前耳机为默认设备时拔出：断开提示应显示耳机名称、Windows 自动回退后的当前设备，以及仍在线的输出设备。下拉选择其他设备并确认，检查切换；不能选择已经断开的耳机。
14. 拔掉非默认设备，确认没有额外提示。关闭新设备接入询问后拔掉默认设备，仍应收到断开提示。
15. 断开全部输出设备，提示应说明没有可用输出设备，显示“知道了”，不能出现可点击的切换操作。重新接入设备后列表和提示应恢复。
16. 断开提示显示期间再次拔出系统回退的默认设备，应更新为新的断开提示；快速插拔不应堆积多个同类提示。输出和输入同时断开时分别保留提示。
17. 接入提示中使用新增的在线设备下拉框选择第三个设备，确认可以直接切换。选中设备后在点击之前拔出，确认刷新选项或报错，不能误切到其他设备。
18. 管理面板关闭时接入设备，确认右下角直接出现带操作按钮的浮窗，没有弹出完整管理面板。分别点击“使用新设备”“继续使用旧设备”“保持系统当前选择”，检查实际默认值。
19. 当前设备断开后，从右下角浮窗的下拉框选中替代设备，直接点击切换；没有可用设备时应只有说明和“知道了”。
20. 输入输出同时发生变化时，浮窗应先处理一项，再显示另一项。所有选择处理完后，任务管理器中提示进程应退出，只保留托盘后台（以及用户主动打开的管理面板）。
21. 点击浮窗 × 后从托盘重新打开待处理提示。浮窗打开时退出托盘程序，确认提示进程也退出。浮窗出现时检查原窗口的键盘焦点没有被抢走。
22. 检查紧凑浮窗：100% 缩放下普通提示约 360 × 211，错误出现时自动增高、按钮仍能点击；分别在 150% 和 200% 缩放下检查文字和按钮不被遮挡。
23. 实际 USB / 蓝牙设备插拔时观察弹出速度，确认没有先显示空白窗口。连续变化不应无限延后首次提示，输出输入仍应保留独立选择；具体总耗时取决于驱动上报时间。
24. 分别为两个输出设备保存不同音量及空间音效，选择“仅保存”，确认系统当前状态不变。重启程序后再次打开设备设置，预设应保留。
25. 从主面板、托盘和右下角浮窗切换同一设备，检查其音量和空间音效均按预设应用。手动改动当前设备音量后点击“应用预设”，应恢复保存值。
26. 当前设备接入提示中选“继续用旧设备”，旧设备自己的预设也应应用；选“保持当前选择”则不改变任何音量或音效。
27. 麦克风的音量预设应可设置，空间音效不可选；静音状态不应因应用音量预设被主动取消。将音量设为 0 后保存并重启，仍应保持 0 的预设。
28. 空间音效关闭、Windows Sonic、已安装激活的 Dolby / DTS 格式分别验证；未激活或不支持的格式失败时应提示原因，不假报成功。
29. 验证保存并应用失败后，预设仍已保存且可修改；切换失败时之前改变的音量和空间音效应恢复，恢复失败应明确提示。
30. 从旧版升级，确认“按设备优先级选择”默认开启，输出／输入初始最高优先级分别是各自当前默认设备。调整顺序后，主面板和托盘菜单应同步排序。
31. 将耳机排在扬声器前，保存后立即使用在线耳机并应用预设。拔出耳机后自动选择扬声器，重新接入后恢复耳机；即使 Windows 已自行选择目标，目标音量／空间音效预设也应应用。
32. 拔出设备后打开“调整优先级”，确认离线设备位置保留且可以上下移动。重启后顺序、开关及预设应保留。新设备出现在末尾。
33. 分别调整输出和输入顺序，确认不会跨类型切换；关闭“同时切换通话设备”后，自动切换不改变通话默认值。
34. 关闭优先级后插拔设备，确认声间只显示原有手动提示，跟随 Windows 默认选择。此时保存排序只改变列表顺序，不改音频设置；重新开启后立即按优先级选择。
35. 开启优先级后手动选择较低优先级设备、在 Windows 中修改默认设备或点击“继续用旧设备”，确认不会被后台立即改回；同类设备下次接入／断开时才重新按顺序选择。
36. 关闭接入询问但保持优先级开启，接入设备仍按优先级处理。断开全部设备不应报错或死循环；重新接入后恢复选择。
37. 排序窗口打开期间接入新设备，保存排序后新增设备不能丢失。模拟预设应用失败时确认显示失败原因、排序仍保存，手动切换仍可用。
38. 将一个低优先级设备设为“接受系统选择，不弹窗”，重新接入并让 Windows 自动选中它：确认不会弹窗，也不会被普通优先级切回。音量／音效维持 Windows 当前值。仅接入而未被系统选中时，应沿用普通处理逻辑。
39. 当前设备断开后，如果 Windows 回退到“接受系统选择”白名单设备，确认不显示该类断开提示。先出现提示、后收到系统默认变化时，提示应自动消失。
40. 将低优先级耳机设为“接入时自动切换，不弹窗”，保存后拔出再接入：应切到耳机、应用其预设且不弹窗；关闭全局优先级及接入询问后再验证一次。
41. 同时接入多个自动切换白名单设备，应按同类优先级选一个；同时接入普通麦克风和白名单输出时，只保留麦克风提示。分别设置输出和输入规则，确认互不影响。
42. 自动切换白名单设备失败时，确认报告错误并保留手动恢复入口；反复收到相同状态通知不应重复切换。重启时已在线设备不应被误判为新接入。
43. 重启检查两种规则仍保留，改为“不加入白名单”后恢复全局逻辑。修改规则不得丢失其他设备的预设、优先级和白名单。
44. 重现 Razer 扬声器问题：保存的音效当前不受支持、设备为自动切换白名单时重新接入，仍应切换到此设备并应用音量，不出现手动选择弹窗或错误气泡；管理面板应说明本次保留当前音效，保存的预设不能被修改。
45. 普通当前设备断开、按优先级回退到已在线的白名单设备时不弹该类断开提示；回退到普通设备时仍保留提示。输出免打扰不能隐藏普通麦克风变化。
46. 白名单设备存在不支持的音效且默认设备切换也失败时，仍应报告真正的切换失败、保留手动选择并恢复本次音量更改；手动点击“应用预设”也应保留严格的失败反馈。
47. 面板出现音效说明后点击右侧“×”，确认提示立即收起，定时刷新、关闭重开面板后均不重现；之后新设备或新的音效问题仍可显示。关闭提示不应改变设备、音量、音效或白名单。
48. 面板同时存在错误和音效说明时，可分别关闭；关闭后一条时不能重新显示已经关闭的前一条。再次主动操作失败时，应显示本次错误。缩小窗口时文字不能遮住关闭按钮。
49. 点击“导出备份”，检查文件包含全部偏好、设备顺序、离线设备、音量为 0／保持音量、关闭／保持空间音效，以及两种白名单。取消文件选择不应写入文件或改变设置。
50. 导入备份，摘要确认后检查全部设置恢复、后台立即更新、当前默认设备与音量不变。原配置自动备份应存在且能够再次导入；重启后导入结果仍保留。
51. 导入无效 JSON、非配置文件、不支持的版本、重复设备、非法音量或白名单时，应显示错误且现有设置不变。取消导入确认同样不得修改配置。
52. 从旧版升级，检查原设置自动迁移至版本 1 格式，迁移前备份存在。导入旧版无版本号文件应成功；离线设备和暂不支持的音效预设应保留。
53. 缩小窗口，检查侧栏“导出备份”“导入设置”按钮可见、可点击；导出覆盖文件应由文件选择框确认，导出不能覆盖活动 settings.json。
# Dolby 每设备方案（v0.9）

- 对支持 DAX 的输出设备，先在 Dolby Access 调好自定义 1，点击声间“读取当前 Dolby 并填入”，仅保存，确认当下声音不变。对另一设备保存不同方案，来回选择两设备，确认预设与 EQ 分别恢复。
- 关闭主面板，从托盘切换；辅助进程应执行完即退出，Dolby Access 不应启动。插拔后按优先级／白名单回退同样应用方案。
- 接受系统选择白名单：系统改为该输出时不弹窗，不改原音量和 Windows 空间音效，但显式开启的 Dolby 方案应生效。
- 快速 A → B → C，最终只应用 C；设备在应用期间断开时停止写入，并在面板显示未完成或恢复结果。只改变通话设备时不要应用 Dolby。
- 不支持的设备、Dolby 服务停止或组件版本更新：配置保留，切换仍可完成，面板有可关闭说明。取消此设备的 Dolby 勾选后不再启动 Dolby 操作。
- 导出后修改方案再导入，确认所有参数与 20 段 EQ 恢复，导入本身不改变声音；下次选择设备再生效。

## v0.10.1：Dolby 读取与取消

- 点击“读取当前 Dolby 并填入”后，确认读取期间 Dolby 编辑区和自动应用复选框不可操作；完成或失败后恢复可操作，失败不清空原方案。
- 读取期间在 Dolby Access 改变主预设或自定义槽位；检测到变化时应提示重新读取，不能把混合结果填入设备方案。
- 快速更换输出时检查旧 Dolby 任务停止写入。驱动读回无效数值／恢复异常由自动故障注入测试覆盖，不在真实设备上故意破坏驱动状态。

## v0.10.0：10 点 dB / 20 段原始值

- 输出设备设置 → Dolby 方案 → 自定义 1 或 2 → 应用均衡器，检查 10 个频率标签及 ±12 dB 范围，输入小数后保存并重新打开。
- 先在高级视图调整一个中间频段，再往返切换视图并“仅保存”；确认原 20 段逐个不变，实际声音不变。
- 修改一个 10 点数值，切到高级视图确认中间段被重新计算；保存后导出、导入，确认 20 段逐个一致。
- 不勾选“应用均衡器”时不可编辑，保存保留“不改变均衡器”的含义。电影模式不应应用自定义 EQ。
- 在受支持的当前输出上“保存并应用”，读取确认 20 段一致；切换到另一个已保存 Dolby 方案的设备，再切回，检查各自曲线恢复。
- 在 100%、150%、200% DPI 下滚动到均衡器，检查两种视图的数值、说明和保存按钮均可访问。

## v0.9.1 回归场景

- Dolby 正在应用时，关闭该设备自动方案并仅保存，或导入另一份配置：旧任务停止继续写入，不应用刚保存／导入的方案；已发生的写入只在原输出仍有效时恢复。退出后台后也不应继续应用旧方案。
- 设备属性临时读取失败时修改白名单并仅保存，检查已保存音量和空间音效仍在；驱动恢复后仍能应用。
- 提示内先遇到一次操作失败，再由白名单或其他窗口处理完设备变化：右下角提示应退出，不留下空错误窗口。
- 触发未支持设备或版本的 Dolby 错误，检查面板中文正常显示，不出现乱码。
